[tool.poetry]
name = "atomcache"
version = "0.1.0"
description = "Asynchronous cache manager designed for horizontally scaled web servers."
authors = ["Serghei Ungurean <srg.ungurean@gmail.com>", "Nichita Morcotilo"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.7"
fastapi = "^0.70.0"
aioredis = "^2.0.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
